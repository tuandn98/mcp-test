# Public tool documentation (reserved)

This directory is reserved for documentation of **non-internal** tool groups (for example tools under a future `src/tools/definitions/public/` or similar).

When that code exists, add overview and per-group files here and link them from [docs/README.md](../README.md) and [api/overview.md](../overview.md).

There is **no public tool group** in the repository at this time.
